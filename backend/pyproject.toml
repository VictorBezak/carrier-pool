[project]
name = "carrier-pool-backend"
version = "0.1.0"
description = "Carrier recommendation engine for freight broker TMS data"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
